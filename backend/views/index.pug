doctype html
html
    head
        meta(name="viewport", content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0")
        title Hello World
    body
        p o hai
        if ENV==="development"
            script(src="http://localhost:3001/static/bundle.js")
        else
            script(src="/bundle.js")